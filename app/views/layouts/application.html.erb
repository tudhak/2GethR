<!DOCTYPE html>
<html>
  <head>
    <title>ToGethR - The best application for couples</title>




    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%# Debut ajout JM : configuration Mobile App %>
    <%# Step 1: Prepare app for A2HS %>
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%# Step 2: Link the manifest to the layout %>
    <link rel="manifest" href="<%= asset_path 'manifest.json' %>">
    <%= render 'shared/ios_pwa_links' %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.css"/>

    <%# Fin ajout JM : configuration Mobile App %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css">
  </head>
  <body class="layout bg_mood <%= action_name == 'show' && controller_name == 'couples' ? @partner.statues.order(:created_at).last.mood_category.title : '' %> <%= action_name == 'show' && controller_name == 'couples' ? 'bg_dynamic' : 'bg_static' %>">

    <% if action_name == 'show' %>
      <div id="pattern" class="bg-layer"></div>
      <div id="noise" class="bg-layer"></div>
    <% end %>
    <div class="py-2">
      <%= render "shared/top_navbar" %>
    </div>
    <div style="height:calc(100vh - 121px); overflow-y:scroll;">
      <%= yield %>
    </div>
  </body>




</html>
