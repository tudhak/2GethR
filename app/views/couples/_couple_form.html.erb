<%= simple_form_for [couple, message], html: { data: { action: "turbo:submit-end->couple-subscription#resetForm" }}, class: "d-flex"  do |f| %>
  <%= f.input :content, label: false, placeholder: "Type your message...", wrapper_html: {class: "flex-grow-1"} %>
  <%= f.submit "Send", class: "btn btn-primary mb-3" %>
<% end %>
